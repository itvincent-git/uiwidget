1.2.2
A 增加item设置为disabled时，菜单项显示为灰

========================================================================================

1.2.1
F 修复item设置为disabled时，仍然可以点击的问题

========================================================================================

1.2.0-SNAPSHOT
M showAt, showAlignTo参数修改：
/**
根据坐标显示 
@param l 左的坐标
@param t 上的坐标 
@param config speed：动画速度，action动画效果，parentMenu父菜单
*/
showAt : function(l, t, config)

/**
根据对齐的目标显示
@param target 对齐的目标 
@param pos 对齐的位置tl-bl, tl-tr, tl-br
@param config offset：偏移量，speed：动画速度，action动画效果，parentMenu父菜单
*/
showAlignTo : function(target, pos, config)

========================================================================================
1.1.1 	2010-08-30
M 调整菜单位置
+ 增加handler

========================================================================================
1.1.0    2009-6-3
M menu重构
+ removeItem：删除菜单项
F 多级菜单时对齐问题
+ 为每个菜单个别设置宽度
M 配置子菜单改为使用对象，原有数组方式弃用，但暂时兼容
M 触发的事件参数修改为handler:function(e, item)
+ item增加id属性
M 当菜单显示时才渲染
M 菜单配置方式改为$.menu({})，原$(document.body).menu({})弃用，但暂时兼容

========================================================================================
1.0.1	 2009-5-11
F 没有handler时点击会出错
M CSS调整

========================================================================================
1.0.0    2009-4-27
菜单组件发布